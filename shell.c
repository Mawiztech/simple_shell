#include <stdio.h>


/**
* main - main function the argument
*
* @argc: print argument
* @argv: print the vector
* Return : return 0
*/

int main(int argc, char argv[])
{
	char betty;

	betty = argv[];

	if (argv[] == 1 && argv[] >= 1)
	{
		printf("%c", argv[]);
	}
	return (0);
}
